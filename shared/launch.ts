// Launch state. While SOFT_OPENING is true the shop is browse-only: Chris tells new visitors,
// a HUD ribbon says so, and checkout is clearly a dry run. Flip to false when Stripe is live.
export const SOFT_OPENING = true;
